
# Architecture

These are all single page HTML apps. Any libraries must be loaded via a CDN.

Never do anything that requires a build step. 

# Git
- do not commit to git unless specifically requested

# Design

@DESIGN-GUIDELINES.md